# 签名配置模板。复制为 keystore.properties 后填入实际值（不要提交到版本控制）。
# CI 走仓库 secrets（KEYSTORE_BASE64 / KEYSTORE_PASSWORD / KEY_ALIAS / KEY_PASSWORD），
# 不使用本文件。缺失时 release 构建会退回默认调试签名。

KEYSTORE_FILE=../dsh-folk.jks
KEYSTORE_PASSWORD=your-keystore-password
KEY_ALIAS=your-key-alias
KEY_PASSWORD=your-key-password
